import 'package:flutter/material.dart';

class AITurnHistoryWidget extends StatelessWidget {
  const AITurnHistoryWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}